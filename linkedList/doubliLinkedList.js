function DoublyLinkedList() {
    let Node = function (ele) {
        this.ele = ele;
        thie.next = null;
        this.prev = null; // 新增
    };
    let length = 0;
    let head = null;
    let tail = null; // 对最后一项的引用
}